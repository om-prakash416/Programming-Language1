#include<stdio.h>
main(){
    int n;
    printf("\n enter any number: ");
    scanf("%d",&n);
    if (n==0)
        printf("\n the value is equal to zero");
        else if(n>0)
        printf("\n the number is positive");
        else
        printf("\n the numner negative");
        return 0;
    
}